Task 1: Step 2 Architecture Diagram

Layered Architecture

Controller Layer
  ├── UserController         ← register, changeEmail, changePassword, getUserById
  ├── AuthController         ← login, logout, refresh, verify
  ├── BillingController      ← subscribe, renew, cancel, getPlans, getPayments
  ├── ContentController      ← create, upload, update, delete, getContents
  └── WatchHistoryController ← recordWatch, getHistory

Service Layer
  ├── UserService           ← business logic ของ User, Profile, UserPreference
  ├── AuthService           ← business logic ของ AuthManager, UserSession
  ├── BillingService        ← business logic ของ Subscription, Plan, Payment
  ├── ContentService        ← business logic ของ Content, Video, Music
  └── WatchHistoryService   ← business logic ของ WatchHistory

Repository Layer
  ├── UserRepository        ← query users table
  ├── ProfileRepository     ← query profiles table
  ├── UserPreferenceRepository ← query user_preferences table
  ├── SessionRepository     ← query sessions table
  ├── PlanRepository        ← query plans table
  ├── SubscriptionRepository← query subscriptions table
  ├── PaymentRepository     ← query payments table
  ├── VideoRepository       ← query contents table WHERE type = Video
  ├── MusicRepository       ← query contents table WHERE type = Music
  └── WatchHistoryRepository← query watch_history table

Domain Layer
  ├── User, Profile, UserPreference
  ├── UserSession, AccessPrincipal
  ├── Plan, Subscription, Payment
  ├── SubscriptionStatus, PaymentStatus
  ├── Content, Video, Music
  └── WatchHistory


Folder Structure Example

Streaming-Subscription-System/
│
├── identity/
│   ├── handler/
│   │   └── UserController.java / user_handler.go
│   ├── service/
│   │   └── UserService.java / user_service.go
│   ├── repository/
│   │   ├── UserRepository.java / user_repository.go
│   │   ├── ProfileRepository.java / profile_repository.go
│   │   └── UserPreferenceRepository.java / user_preference_repository.go
│   └── domain/
│       ├── User.java / user.go
│       ├── Profile.java / profile.go
│       └── UserPreference.java / user_preference.go
│
├── auth/
│   ├── handler/
│   │   └── AuthController.java / auth_handler.go
│   ├── service/
│   │   └── AuthService.java / auth_service.go
│   ├── repository/
│   │   └── SessionRepository.java / session_repository.go
│   └── domain/
│       ├── UserSession.java / user_session.go
│       └── AccessPrincipal.java / access_principal.go
│
├── billing/
│   ├── handler/
│   │   └── BillingController.java / billing_handler.go
│   ├── service/
│   │   └── BillingService.java / billing_service.go
│   ├── repository/
│   │   ├── PlanRepository.java / plan_repository.go
│   │   ├── SubscriptionRepository.java / subscription_repository.go
│   │   └── PaymentRepository.java / payment_repository.go
│   └── domain/
│       ├── Plan.java / plan.go
│       ├── Subscription.java / subscription.go
│       ├── Payment.java / payment.go
│       └── Enums.java / enums.go
│           └── SubscriptionStatus, PaymentStatus
│
└── content/
    ├── handler/
    │   ├── ContentController.java / content_handler.go
    │   └── WatchHistoryController.java / watch_history_handler.go
    ├── service/
    │   ├── ContentService.java / content_service.go
    │   └── WatchHistoryService.java / watch_history_service.go
    ├── repository/
    │   ├── VideoRepository.java / video_repository.go
    │   ├── MusicRepository.java / music_repository.go
    │   └── WatchHistoryRepository.java / watch_history_repository.go
    └── domain/
        ├── Content.java / content.go
        ├── Video.java / video.go
        ├── Music.java / music.go
        └── WatchHistory.java / watch_history.go


Task 2: Step 3 Contract

Identity

1.) POST /api/identity/users   //สมัครสมาชิกใหม่

Request
{
  "email": "somchai@gmail.com",
  "password": "pass123"
}

java
public interface UserService {
    User register(String email, String password);
}

golang
type UserService interface {
    Register(email string, password string) (*User, error)
}

Expected Response
{
  "userId": "u-001",
  "email": "somchai@gmail.com"
}

-----------------------------------------------------------------
2.) GET /api/identity/users/me   //ดึงข้อมูล user ของตัวเอง

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login)

java
public interface UserService {
    User getUserById(String userId);
}

golang
type UserService interface {
    GetUserByID(userID string) (*User, error)
}

Expected Response
{
  "userId": "u-001",
  "email": "somchai@gmail.com"
}

-----------------------------------------------------------------
3.) PATCH /api/identity/users/me/email   //เปลี่ยน email ต้องยืนยันตัวตนผ่าน token ก่อน

Request
{
  "newEmail": "somchai.new@gmail.com"
}

java
public interface UserService {
    boolean changeEmail(String userId, String newEmail);
}

golang
type UserService interface {
    ChangeEmail(userID string, newEmail string) (bool, error)
}

Expected Response
{
  "userId": "u-001",
  "email": "somchai.new@gmail.com"
}

-----------------------------------------------------------------
4.) PATCH /api/identity/users/me/password   //เปลี่ยน password ต้องใส่รหัสเดิมก่อนเพื่อยืนยันว่าเป็นเจ้าของจริง

Request
{
  "oldPassword": "pass123",
  "newPassword": "newpass456"
}

java
public interface UserService {
    boolean changePassword(String userId, String oldPassword, String newPassword);
}

golang
type UserService interface {
    ChangePassword(userID string, oldPassword string, newPassword string) (bool, error)
}

Expected Response
{
  "message": "เปลี่ยน password สำเร็จ"
}

-----------------------------------------------------------------
5.) GET /api/identity/users/me/profile   //ดึงข้อมูล profile สำหรับแสดงบนหน้าจอ เช่น ชื่อและรูปโปรไฟล์

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login)

java
public interface UserService {
    Profile getProfileByUserId(String userId);
}

golang
type UserService interface {
    GetProfileByUserID(userID string) (*Profile, error)
}

Expected Response
{
  "userId": "u-001",
  "displayName": "สมเพช",
  "avatarUrl": "cdn.example.com/avatars/u-001.jpg"
}

-----------------------------------------------------------------
6.) PATCH /api/identity/users/me/profile   //แก้ไขข้อมูล profile เช่น เปลี่ยนชื่อที่แสดงหรือรูปโปรไฟล์

Request
{
  "displayName": "สมเพช ใหม่",
  "avatarUrl": "cdn.example.com/avatars/u-001-new.jpg"
}

java
public interface UserService {
    boolean updateProfile(String userId, String displayName, String avatarUrl);
}

golang
type UserService interface {
    UpdateProfile(userID string, displayName string, avatarUrl string) (bool, error)
}

Expected Response
{
  "userId": "u-001",
  "displayName": "สมเพช ใหม่",
  "avatarUrl": "cdn.example.com/avatars/u-001-new.jpg"
}

-----------------------------------------------------------------
7.) GET /api/identity/users/me/preferences   //ดึง preference ทั้งหมดของ user เช่น ภาษา ธีม การเล่นอัตโนมัติ

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login)

java
public interface UserService {
    List<UserPreference> getAllUserPreferences(String userId);
}

golang
type UserService interface {
    GetAllUserPreferences(userID string) ([]*UserPreference, error)
}

Expected Response
[
  { "prefKey": "language",  "prefValue": "th"   },
  { "prefKey": "theme",     "prefValue": "dark" },
  { "prefKey": "autoplay",  "prefValue": "true" }
]

-----------------------------------------------------------------
8.) GET /api/identity/users/me/preferences/{prefKey}   //ดึง preference ตาม key ที่ระบุ ใช้ตอนต้องการค่าเดียวเจาะจง

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login)

java
public interface UserService {
    String getUserPreference(String userId, String prefKey);
}

golang
type UserService interface {
    GetUserPreference(userID string, prefKey string) (string, error)
}

Expected Response
{
  "prefKey":   "theme",
  "prefValue": "dark"
}

-----------------------------------------------------------------
9.) PUT /api/identity/users/me/preferences/{prefKey}   //set หรืออัปเดต preference ถ้ามี key อยู่แล้วให้อัปเดต ถ้าไม่มีให้สร้างใหม่

Request
{
  "prefValue": "light"
}

java
public interface UserService {
    boolean setUserPreference(String userId, String prefKey, String prefValue);
}

golang
type UserService interface {
    SetUserPreference(userID string, prefKey string, prefValue string) (bool, error)
}

Expected Response
{
  "prefKey":   "theme",
  "prefValue": "light"
}

-----------------------------------------------------------------
10.) DELETE /api/identity/users/me/preferences/{prefKey}   //ลบ preference นั้นออก ใช้ตอน reset settings กลับค่าเริ่มต้น

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login)

java
public interface UserService {
    boolean removeUserPreference(String userId, String prefKey);
}

golang
type UserService interface {
    RemoveUserPreference(userID string, prefKey string) (bool, error)
}

Expected Response
{
  "message": "ลบ preference สำเร็จ"
}

*****************************************************************************

Auth

11.) POST /api/auth/login   //รับ email และ password แล้วคืน token สำหรับใช้งาน API ทั้งหมด

Request
{
  "email": "somchai@gmail.com",
  "password": "pass123"
}

java
public interface AuthService {
    UserSession login(String email, String password);
}

golang
type AuthService interface {
    Login(email string, password string) (*UserSession, error)
}

Expected Response
{
  "sessionId":    "s-001",
  "accessToken":  "eyJ...",
  "refreshToken": "rt_xK9...",
  "expiresAt":    "2024-01-15T09:15:00"
}

-----------------------------------------------------------------
12.) POST /api/auth/logout   //ยกเลิก session ทันที token ที่ใช้อยู่จะใช้งานไม่ได้อีกเลย

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login)

java
public interface AuthService {
    boolean logout(String accessToken);
}

golang
type AuthService interface {
    Logout(accessToken string) (bool, error)
}

Expected Response
{
  "message": "logout สำเร็จ"
}

-----------------------------------------------------------------
13.) POST /api/auth/refresh   //ขอ token ชุดใหม่โดยใช้ refreshToken token เก่าจะถูกทำลายทันที

Request
{
  "refreshToken": "rt_xK9..."  -->  (จาก login)
}

java
public interface AuthService {
    UserSession refresh(String refreshToken);
}

golang
type AuthService interface {
    Refresh(refreshToken string) (*UserSession, error)
}

Expected Response
{
  "sessionId":    "s-002",
  "accessToken":  "eyJ...ใหม่",
  "refreshToken": "rt_abc...ใหม่",
  "expiresAt":    "2024-01-15T10:15:00"
}

-----------------------------------------------------------------
14.) GET /api/auth/verify   //ตรวจสอบว่า token ยังใช้งานได้ไหม ในทางปฏิบัติมักใช้เป็น middleware ไม่ใช่ endpoint ตรงๆ

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)

java
public interface AuthService {
    AccessPrincipal verify(String accessToken);
}

golang
type AuthService interface {
    Verify(accessToken string) (*AccessPrincipal, error)
}

Expected Response
{
  "userId":      "u-001",
  "permissions": ["upload_content", "watch_content"],
  "hasActiveSubscription": true
}

*****************************************************************************

Billing

15.) GET /api/billing/plans   //ดึง plan ทั้งหมดสำหรับแสดงหน้าเลือก plan ก่อนสมัคร

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)

java
public interface BillingService {
    List<Plan> getAllPlans();
}

golang
type BillingService interface {
    GetAllPlans() ([]*Plan, error)
}

Expected Response
[
  {
    "planId":       "p-001",
    "name":         "Monthly",
    "price":        199.00,
    "billingCycle": "monthly"
  },
  {
    "planId":       "p-002",
    "name":         "Annual",
    "price":        1990.00,
    "billingCycle": "annual"
  }
]

-----------------------------------------------------------------
16.) GET /api/billing/plans/{planId}   //ดึงรายละเอียดของ plan นั้นๆ ใช้ตอนต้องการข้อมูลเจาะจง

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  planId = "p-001"

java
public interface BillingService {
    Plan getPlanById(String planId);
}

golang
type BillingService interface {
    GetPlanByID(planID string) (*Plan, error)
}

Expected Response
{
  "planId":       "p-001",
  "name":         "Monthly",
  "price":        199.00,
  "billingCycle": "monthly"
}

-----------------------------------------------------------------
17.) GET /api/billing/subscriptions/me   //ดูสถานะ subscription ปัจจุบันของตัวเอง รวมถึงรายละเอียด plan ที่ใช้อยู่

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)

java
public interface BillingService {
    Subscription getSubscriptionByUserId(String userId);
}

golang
type BillingService interface {
    GetSubscriptionByUserID(userID string) (*Subscription, error)
}

Expected Response
{
  "subscriptionId": "sub-001",
  "status":         "ACTIVE",
  "startDate":      "2024-01-15",
  "endDate":        "2024-02-15",
  "currentPlan": {
    "planId":       "p-001",
    "name":         "Monthly",
    "price":        199.00,
    "billingCycle": "monthly"
  }
}

-----------------------------------------------------------------
18.) POST /api/billing/subscriptions   //สมัคร subscription คืน URL สำหรับ redirect ไปจ่ายเงินที่ Stripe/Omise(ภายนอก)

Request
{
  "planId": "p-001"
}

java
public interface BillingService {
    String subscribe(String userId, String planId);
}

golang
type BillingService interface {
    Subscribe(userID string, planID string) (string, error)
}

Expected Response
{
  "subscriptionId": "sub-001",
  "status":         "PENDING",
  "checkoutUrl":    "https://checkout.stripe.com/..."
}

-----------------------------------------------------------------
19.) POST /api/billing/subscriptions/me/renew   //ต่ออายุ subscription คืน URL สำหรับ redirect ไปจ่ายเงินอีกครั้ง

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)

java
public interface BillingService {
    String renew(String userId, String subscriptionId);
}

golang
type BillingService interface {
    Renew(userID string, subscriptionID string) (string, error)
}

Expected Response
{
  "subscriptionId": "sub-001",
  "checkoutUrl":    "https://checkout.stripe.com/..."
}

-----------------------------------------------------------------
20.) DELETE /api/billing/subscriptions/me   //ยกเลิก subscription ของตัวเอง status จะเปลี่ยนเป็น CANCELLED ทันที

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)

java
public interface BillingService {
    boolean cancel(String userId);
}

golang
type BillingService interface {
    Cancel(userID string) (bool, error)
}

Expected Response
{
  "subscriptionId": "sub-001",
  "status":         "CANCELLED"
}

-----------------------------------------------------------------
21.) GET /api/billing/payments   //ดูประวัติการจ่ายเงินทั้งหมดของตัวเอง เรียงจากล่าสุดไปเก่าสุด

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)

java
public interface BillingService {
    List<Payment> getPaymentsBySubscriptionId(String subscriptionId);
}

golang
type BillingService interface {
    GetPaymentsBySubscriptionID(subscriptionID string) ([]*Payment, error)
}

Expected Response
[
  {
    "paymentId":             "pay-001",
    "status":                "COMPLETED",
    "amount":                199.00,
    "currency":              "THB",
    "paidAt":                "2024-01-15T09:00:00"
  },
  {
    "paymentId":             "pay-002",
    "status":                "FAILED",
    "amount":                199.00,
    "currency":              "THB",
    "paidAt":                null
  }
]

-----------------------------------------------------------------
22.) GET /api/billing/payments/{paymentId}   //ดูรายละเอียดของ payment รายการนั้นเจาะจง

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  paymentId = "pay-001"

java
public interface BillingService {
    Payment getPaymentById(String paymentId);
}

golang
type BillingService interface {
    GetPaymentByID(paymentID string) (*Payment, error)
}

Expected Response
{
  "paymentId":              "pay-001",
  "subscriptionId":         "sub-001",
  "externalTransactionId":  "pi_stripe_abc123",
  "status":                 "COMPLETED",
  "amount":                 199.00,
  "currency":               "THB",
  "paidAt":                 "2024-01-15T09:00:00"
}

-----------------------------------------------------------------
23.) POST /api/billing/payments/webhook   //รับแจ้งผลการจ่ายเงินจาก Stripe/Omise อัตโนมัติ ไม่ได้เรียกโดย user
โครงสร้างขึ้นอยู่ Body ขึ้นอยู่กับผู้ให้บริการภายนอก

Request
Header: Stripe-Signature: "t=...,v1=..."  (จากภายนอกเช่นเลขทธุรกรรม)
Body: {
  "type": "payment.success",
  "data": {
    "externalTransactionId": "pi_stripe_abc123"
  }
}

java
public interface BillingService {
    boolean handleWebhook(Object payload);
}

golang
type BillingService interface {
    HandleWebhook(payload interface{}) (bool, error)
}

Expected Response
{
  "message": "webhook รับสำเร็จ"
}

-----------------------------------------------------------------
24.) POST /api/billing/payments/{paymentId}/refund   //ขอคืนเงินสำหรับ payment ที่ COMPLETED แล้วเท่านั้น

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  paymentId = "pay-001"

java
public interface BillingService {
    boolean refund(String paymentId);
}

golang
type BillingService interface {
    Refund(paymentID string) (bool, error)
}

Expected Response
{
  "paymentId": "pay-001",
  "status":    "REFUNDED"
}

*****************************************************************************

Content

25.) GET /api/content/contents   //ดึง content ทั้งหมดสำหรับแสดงหน้า browse ทั้ง Video และ Music

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)

java
public interface ContentService {
    List<Content> getAllContents();
}

golang
type ContentService interface {
    GetAllContents() ([]*Content, error)
}

Expected Response
[
  {
    "contentId":  "v-001",
    "creatorId":  "u-123",
    "title":      "วันสบายๆ ริมทะเล",
    "type":       "Video",
    "resolution": "1080p",
    "duration":   3600,
    "codec":      "H.264"
  },
  {
    "contentId": "m-001",
    "creatorId": "u-456",
    "title":     "สายลม",
    "type":      "Music",
    "artist":    "Bodyslam",
    "bitrate":   320,
    "genre":     "Rock"
  }
]

-----------------------------------------------------------------
26.) GET /api/content/contents/{contentId}   //ดึงรายละเอียด content นั้น พร้อม signed URL สำหรับเล่นทันที

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  contentId = "v-001"

java
public interface ContentService {
    Content getContentById(String contentId, String userId);
}

golang
type ContentService interface {
    GetContentByID(contentID string, userID string) (*Content, error)
}

Expected Response
{
  "contentId":  "v-001",
  "creatorId":  "u-123",
  "title":      "วันสบายๆ ริมทะเล",
  "cdnUrl":     "https://cdn.../v-001.mp4?token=xyz&expires=...",
  "type":       "Video",
  "resolution": "1080p",
  "duration":   3600,
  "codec":      "H.264"
}

-----------------------------------------------------------------
27.) GET /api/content/contents/type/{type}   //กรอง content ตามประเภท ใช้ตอนอยากดูแค่ Video หรือแค่ Music

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  type = "Video" หรือ "Music"

java
public interface ContentService {
    List<Content> getContentsByType(String type);
}

golang
type ContentService interface {
    GetContentsByType(contentType string) ([]*Content, error)
}

Expected Response
[
  {
    "contentId":  "v-001",
    "creatorId":  "u-123",
    "title":      "วันสบายๆ ริมทะเล",
    "type":       "Video",
    "resolution": "1080p",
    "duration":   3600,
    "codec":      "H.264"
  },
  {
    "contentId":  "v-002",
    "creatorId":  "u-456",
    "title":      "เที่ยวทะเล",
    "type":       "Video",
    "resolution": "720p",
    "duration":   1800,
    "codec":      "H.265"
  }
]

-----------------------------------------------------------------
28.) GET /api/content/contents/creator/{creatorId}   //ดึง content ทั้งหมดของ creator นั้น ใช้ตอนเปิดหน้าโปรไฟล์ creator

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  creatorId = "u-123"

java
public interface ContentService {
    List<Content> getContentsByCreatorId(String creatorId);
}

golang
type ContentService interface {
    GetContentsByCreatorID(creatorID string) ([]*Content, error)
}

Expected Response
[
  {
    "contentId":  "v-001",
    "creatorId":  "u-123",
    "title":      "วันสบายๆ ริมทะเล",
    "type":       "Video",
    "resolution": "1080p",
    "duration":   3600,
    "codec":      "H.264"
  },
  {
    "contentId":  "v-002",
    "creatorId":  "u-123",
    "title":      "เที่ยวทะเล",
    "type":       "Video",
    "resolution": "720p",
    "duration":   1800,
    "codec":      "H.265"
  }
]

-----------------------------------------------------------------
29.) POST /api/content/contents   //สร้าง content record ใหม่ก่อน แล้วค่อยอัปโหลดไฟล์ภายหลัง

Request
{
  "title":      "วันสบายๆ ริมทะเล",
  "type":       "Video",
  "resolution": "1080p",
  "duration":   3600,
  "codec":      "H.264"
}

java
public interface ContentService {
    Content createContent(String userId, String title, String type, Map<String, Object> metadata);
}

golang
type ContentService interface {
    CreateContent(userID string, title string, contentType string, metadata map[string]interface{}) (*Content, error)
}

Expected Response
{
  "contentId":  "v-001",
  "creatorId":  "u-123",
  "title":      "วันสบายๆ ริมทะเล",
  "cdnUrl":     null,
  "type":       "Video",
  "resolution": "1080p",
  "duration":   3600,
  "codec":      "H.264"
}

-----------------------------------------------------------------
30.) PATCH /api/content/contents/{contentId}   //แก้ไขข้อมูล content เฉพาะเจ้าของเท่านั้น

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  contentId = "v-001"
{
  "title": "วันสบายๆ ริมทะเล (แก้ไขแล้ว)"
}

java
public interface ContentService {
    boolean updateContent(String userId, String contentId, String title);
}

golang
type ContentService interface {
    UpdateContent(userID string, contentID string, title string) (bool, error)
}

Expected Response
{
  "contentId":  "v-001",
  "creatorId":  "u-123",
  "title":      "วันสบายๆ ริมทะเล (แก้ไขแล้ว)",
  "type":       "Video",
  "resolution": "1080p",
  "duration":   3600,
  "codec":      "H.264"
}

-----------------------------------------------------------------
31.) DELETE /api/content/contents/{contentId}   /ลบ content ออกจากระบบทั้งไฟล์บน CDN และ record ใน DB เฉพาะเจ้าของเท่านั้น

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  contentId = "v-001"

java
public interface ContentService {
    boolean deleteContent(String userId, String contentId);
}

golang
type ContentService interface {
    DeleteContent(userID string, contentID string) (bool, error)
}

Expected Response
{
  "message": "ลบ content สำเร็จ"
}

-----------------------------------------------------------------
32.) POST /api/content/contents/{contentId}/upload   //อัปโหลดไฟล์จริงขึ้น CDN หลังจากสร้าง content record ไปแล้ว

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
Param:  contentId = "v-001"
Body:   multipart/form-data { file: video.mp4 }

java
public interface ContentService {
    String uploadContent(String userId, String contentId, MultipartFile file);
}

golang
type ContentService interface {
    UploadContent(userID string, contentID string, file multipart.File) (string, error)
}

Expected Response
{
  "contentId": "v-001",
  "cdnUrl":    "cdn.example.com/videos/v-001.mp4"
}

-----------------------------------------------------------------
33.) POST /api/content/watch-history   //บันทึกการเข้าชม content ทุกครั้งที่ user กดเล่น

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)
{
  "contentId": "v-001"
}

java
public interface WatchHistoryService {
    WatchHistory recordWatchHistory(String userId, String contentId);
}

golang
type WatchHistoryService interface {
    RecordWatchHistory(userID string, contentID string) (*WatchHistory, error)
}

Expected Response
{
  "historyId":       "h-001",
  "userId":          "u-123",
  "contentId":       "v-001",
  "watchedAt":       "2024-01-15T09:00:00",
  "watchedDuration": 0
}

-----------------------------------------------------------------
34.) GET /api/content/watch-history/me   //ดูประวัติการรับชมทั้งหมดของตัวเอง เรียงจากล่าสุดไปเก่าสุด

Request
Header: Authorization: Bearer "eyJ..."    -->  ("accessToken" จาก login หรือ refresh)

java
public interface WatchHistoryService {
    List<WatchHistory> getWatchHistoryByUserId(String userId);
}

golang
type WatchHistoryService interface {
    GetWatchHistoryByUserID(userID string) ([]*WatchHistory, error)
}

Expected Response
[
  {
    "historyId":       "h-001",
    "contentId":       "v-001",
    "title":           "วันสบายๆ ริมทะเล",
    "type":            "Video",
    "watchedAt":       "2024-01-15T09:00:00",
    "watchedDuration": 3600
  },
  {
    "historyId":       "h-002",
    "contentId":       "m-001",
    "title":           "สายลม",
    "type":            "Music",
    "watchedAt":       "2024-01-15T10:30:00",
    "watchedDuration": 180
  }
]





Table Design

กลุ่ม IDENTITY
1) users table
- userId       STRING
- email        STRING
- passwordHash STRING

2) profiles table
- userId      STRING
- displayName STRING
- avatarUrl   STRING

3) userPreferences table
- prefId    STRING
- userId    STRING
- prefKey   STRING
- prefValue STRING


กลุ่ม AUTH
4) userSessions table
- sessionId    STRING
- userId       STRING
- accessToken  STRING
- refreshToken STRING
- issuedAt     DATE
- expiresAt    DATE


กลุ่ม BILLING
5) plans table
- planId        STRING
- name          STRING
- tier          STRING
- price         FLOAT
- billingCycle  STRING
- maxDevices    INT
- maxResolution STRING
- isActive      BOOLEAN

6) subscriptions table
- subscriptionId STRING
- userId         STRING
- planId         STRING
- status         STRING
- startDate      DATE
- endDate        DATE

7) payments table
- paymentId             STRING
- subscriptionId        STRING
- externalTransactionId STRING
- status                STRING
- amount                FLOAT
- currency              STRING
- paidAt                DATE
- deletedAt             DATE


กลุ่ม CONTENT
8) videos table
- contentId  STRING
- title      STRING
- cdnUrl     STRING
- resolution STRING
- duration   INT
- codec      STRING

9) music table
- contentId STRING
- title     STRING
- cdnUrl    STRING
- artist    STRING
- bitrate   INT
- genre     STRING